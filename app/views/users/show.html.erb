<%= render 'shared/content_title', title: "Profile: #{@user.username}" %>

<div class='well'>
  <dl class='dl-horizontal'>
    <dt>Username:</dt>
    <dd><%= @user.username %></dd>
    <dt>Quizzes taken:</dt>
    <dd><%= @user.user_quizzes.size %></dd>    
  </dt>
</div>

<h5><%= @user.username %>'s Results</h5>

  <% @user.user_quizzes.each do |user_quiz| %>
    <div class=' btn btn-default btn-lg btn-block'>
      <p>
          Quiz Category: <%= @quiz.find(user_quiz.quiz_id).category %>
      </br>
          Score: <%= user_quiz.score %>
      </br>
          Date: <%= display_datetime(user_quiz.created_at) %>
    </div>
  <% end %>



